* Provisioned 2 compute instances
    * Enabled http in both of them 
    * For the backend instance
        * Installed mongo
    * For the front end instance
        * Cloned a node.js application that has an angular client